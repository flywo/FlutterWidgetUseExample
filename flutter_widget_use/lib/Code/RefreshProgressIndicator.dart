import 'package:flutter/material.dart';

class RefreshProgressIndicatorDemo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: RefreshProgressIndicator(),
    );
  }
}